import OrderDetailSkeleton from "@/components/OrderDetailSkeleton";

// 보드와 모든 자식 탭(코드 변경·구현 내용·검증·Jira·설계·회고·아티팩트)이 이 골격을 공유한다.
export default function Loading() {
  return <OrderDetailSkeleton />;
}
